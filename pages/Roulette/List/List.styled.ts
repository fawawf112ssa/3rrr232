import styled from 'styled-components';

export const Wrapper = styled.div`
  margin: 0 auto;
  width: 100%;
  overflow: hidden;
  position: relative;
  border-radius: 5px;
`;

export const RouletteItems = styled.div`
  position: relative;
  left: 0;
  display: flex;
  flex-wrap: nowrap;
`;
